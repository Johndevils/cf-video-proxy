{
  "name": "cf-video-proxy",
  "version": "1.0.0",
  "description": "Single-file Cloudflare Worker video player",
  "main": "index.js",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
